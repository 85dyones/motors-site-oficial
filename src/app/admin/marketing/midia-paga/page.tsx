import MidiaPagaConsolidado from "../../../../components/marketing/MidiaPagaConsolidado";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Mídia Paga — Motors Showcase",
  description: "Meta e Google no mesmo painel: investimento, lead e custo por lead.",
};

export default function MidiaPagaPage() {
  return <MidiaPagaConsolidado />;
}
