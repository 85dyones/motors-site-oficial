import RelatorioDoFunil from "../../../../components/admin/RelatorioDoFunil";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Ganhos e perdas — Motors Showcase",
  description: "Por que a loja fecha e por que deixa de fechar, por motivo, vendedor e período.",
};

export default function RelatorioPage() {
  return <RelatorioDoFunil />;
}
